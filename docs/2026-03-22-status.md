# Project Status Snapshot

**Date:** 2026-03-22
**Branch:** `master`
**Main branch:** `master`

## What This Is

A production-ready, static-first TypeScript web application boilerplate targeting
Cloudflare Workers with Hugo for static pages. Clean Architecture, strict TDD,
and automated spec-to-deploy workflow. Authentication fully integrated via
better-auth with email/password, CSRF protection, rate limiting via Durable Objects,
and session management on D1. Now includes a comprehensive 20-skill design system
imported from Impeccable Design with full Apache 2.0 compliance.

---

## Runtime Stack

| Layer       | Technology                               | Version                       |
| ----------- | ---------------------------------------- | ----------------------------- |
| Compute     | Cloudflare Workers                       | compatibility_date 2025-01-01 |
| Framework   | Hono                                     | 4.12.8                        |
| Auth        | better-auth + Drizzle ORM                | 1.5.5 / 0.45.1                |
| Static site | Hugo (extended)                          | 0.154.5                       |
| CSS         | TailwindCSS 4 + DaisyUI 5                | 4.1 / 5.0.43                  |
| Client JS   | HTMX 2.0.8, Alpine.js 3.15.8             | vendored                      |
| Language    | TypeScript                               | 5.9.3 (extremely strict)      |
| Tests       | Vitest + @cloudflare/vitest-pool-workers | 4.1.0 / 0.13.2                |
| Node        | 22.x (.nvmrc)                            |                               |

## Cloudflare Bindings

| Binding              | Type                   | Purpose                               |
| -------------------- | ---------------------- | ------------------------------------- |
| `ASSETS`             | Fetcher                | Hugo static files from `hugo/public/` |
| `DB`                 | D1Database             | User/session storage (better-auth)    |
| `RATE_LIMIT`         | DurableObjectNamespace | Atomic rate limiting (RateLimitDO)    |
| `BETTER_AUTH_URL`    | string (secret)        | Public base URL                       |
| `BETTER_AUTH_SECRET` | string (secret)        | >= 32 bytes entropy                   |

---

## Architecture (Clean Architecture, 4 Layers)

```
src/
  domain/          Pure business logic, zero external deps
    entities/        AuthUser, AuthSession
    value-objects/   CommonPasswords (OWASP blocklist)
    services/        PBKDF2 password hasher (Web Crypto)

  application/     Use-case orchestration
    ports/           AuthService, RateLimiter, Logger (interfaces only)
    use-cases/       SignInUseCase, SignUpUseCase, SignOutUseCase

  infrastructure/  Adapters implementing ports
    BetterAuthService     Maps better-auth responses to Result types
    DurableObjectRateLimiter  DO-backed atomic rate limiting
    RateLimitDO           Durable Object class for rate limit state
    ConsoleLogger         Logger port implementation
    auth.ts               better-auth singleton factory (D1 + Drizzle)
    tokenHasher.ts        Token hashing utility
    d1DateProxy.ts        D1 date handling adapter

  presentation/    HTTP layer
    handlers/        ApiHandlers, AuthPageHandlers, StaticAssetHandler, AppPartialHandlers
    middleware/      requireAuth (session + CSRF), apiAuthRateLimit
    templates/       authLayout, login, register, rateLimit, authError pages
    utils/           securityHeaders, cookieBuilder, extractClientIp,
                     redirectValidator, html, timingSafeEqual, authErrorStatus
    generated/       assetPaths.ts (synced from Hugo build)

  di/              Composition root
    serviceFactory.ts  Lazy singleton wiring per isolate

  shared/          Cross-cutting utilities
    env.ts           Env interface (Cloudflare bindings)
    hex.ts           Binary-to-hex conversion
    authCookieNames.ts  Cookie name constants

  worker.ts        Hono app entry point
  index.ts         Re-export
```

**Dependency rule enforced:** Domain <- Application <- Infrastructure/Presentation.
ESLint architecture boundary rules via eslint-plugin-boundaries.

---

## Routes (worker.ts)

| Method   | Path                   | Handler                 | Auth / Rate Limit            |
| -------- | ---------------------- | ----------------------- | ---------------------------- |
| GET      | `/api/health`          | healthCheck             | No                           |
| GET      | `/api/auth/error`      | authErrorPage           | No                           |
| GET      | `/api/auth/callback/*` | apiNotFound (blocks)    | No                           |
| GET/POST | `/api/auth/*`          | better-auth passthrough | No                           |
| ALL      | `/api/*`               | apiNotFound (404)       | No                           |
| GET      | `/auth/sign-in`        | login form              | No                           |
| POST     | `/auth/sign-in`        | sign-in                 | signInApiRateLimitMiddleware |
| GET      | `/auth/sign-up`        | register form           | No                           |
| POST     | `/auth/sign-up`        | sign-up                 | signUpApiRateLimitMiddleware |
| POST     | `/auth/sign-out`       | sign-out                | CSRF                         |
| ALL      | `/app/_/*`             | appPartialNotFound      | requireAuth                  |
| ALL      | `/app/*`               | (middleware only)       | requireAuth                  |
| ALL      | `/dashboard/*`         | (middleware only)       | requireAuth                  |
| ALL      | `*`                    | staticAssetFallthrough  | No                           |

**Middleware chain:** Security headers on all routes. Auth guard on `/app/*` and `/dashboard/*`.
Rate limiting on sign-in and sign-up API endpoints via Durable Objects.

---

## Security Posture

- **CSRF:** Double-submit token (form field + `__Secure-csrf` cookie), session-bound via HMAC-SHA256
- **Rate limiting:** Durable Object-backed atomic rate limiting, separate windows for sign-in (15 min) and sign-up (5 min)
- **Timing oracle defense:** PBKDF2 dummy hash on every non-rate-limited failure
- **Email enumeration prevention:** Same response for email_taken and success on sign-up
- **Password validation:** 12-128 chars (better-auth) + OWASP common-password blocklist
- **Sessions:** `__Host-` prefixed cookies, HttpOnly, Secure, SameSite, 30-day TTL
- **Headers:** HSTS (2yr, preload), CSP, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy
- **HTML escaping:** Tagged template literal with auto-escape (`html` utility)
- **Redirect validation:** Blocks `//`, `/api/`, `/auth/` paths; localhost-base URL parsing
- **IP extraction:** CF-Connecting-IP only (ignores X-Forwarded-For to prevent spoofing)
- **OAuth callback blocking:** Unconfigured OAuth callback routes return 404

---

## Database Schema (D1, migrations/)

Single migration: `0001_better_auth_schema.sql`

| Table          | Purpose                                                                  |
| -------------- | ------------------------------------------------------------------------ |
| `user`         | id, name, email (unique), emailVerified, image, timestamps               |
| `session`      | id, userId (FK cascade), token (unique), expiresAt, ipAddress, userAgent |
| `account`      | id, userId (FK cascade), providerId, OAuth tokens, password              |
| `verification` | id, identifier, value, expiresAt (email verify/reset)                    |

All timestamps stored as TEXT in ISO 8601 UTC.

---

## Hugo Static Site

- **Theme:** Custom, DaisyUI 5 "lemonade" palette (oklch colors)
- **Layouts (16):** baseof, home (hero from YAML data), single (with optional TOC), list (paginated card grid), dashboard/list, 404
- **Partials:** header, footer, pagination, hero block, primary/outline buttons
- **SEO:** OpenGraph, Twitter Cards, JSON-LD (WebSite, Article, BreadcrumbList), Google Analytics (optional)
- **Content types:** Pages, posts (with tags and series taxonomies)
- **Security headers:** `static/_headers` for Cloudflare (HSTS, CSP, cache control)
- **Asset fingerprinting:** Hugo Pipes with SRI integrity hashes
- **TailwindCSS scans:** Both Hugo templates and `src/presentation/templates/` for class extraction
- **Build verification:** 8 checks in `test-build.js` (zero-warning policy)

### Content Pages

| Path                                | Purpose                    |
| ----------------------------------- | -------------------------- |
| `hugo/content/_index.md`            | Home page                  |
| `hugo/content/dashboard/_index.md`  | Dashboard (auth-protected) |
| `hugo/content/posts/_index.md`      | Posts listing              |
| `hugo/content/posts/sample-post.md` | Sample blog post           |

---

## Scripts

| File                                        | Purpose                                                |
| ------------------------------------------- | ------------------------------------------------------ |
| `sprite-dev.sh`                             | Runs `just dev` from project root                      |
| `build-template.sh`                         | Builds gzip-compressed tmplr template to dist/         |
| `check-migration-sync.ts`                   | Validates inlined D1 queries match migration SQL       |
| `check-route-collisions.js`                 | Ensures Hugo output doesn't collide with Worker routes |
| `sync-asset-paths.js`                       | Extracts fingerprinted CSS path from Hugo build        |
| `detect-flaky-tests.ts`                     | Analyzes Vitest JSON report for flaky tests            |
| `install-tmplr.sh`                          | Downloads and verifies tmplr binary from GitHub        |
| `verify-test-stability.sh`                  | Runs acceptance tests 10x to verify stability          |
| `__tests__/architecture-boundaries.spec.ts` | Architecture boundary enforcement tests                |

---

## Typing, Linting, and Testing Regimen

### TypeScript Configuration (Extremely Strict)

**tsconfig.json** — ES2022 target, NodeNext modules, `lib: ["ES2022", "WebWorker"]`

All strict flags enabled:

| Flag                                 | Value   |
| ------------------------------------ | ------- |
| `strict`                             | `true`  |
| `noImplicitAny`                      | `true`  |
| `strictNullChecks`                   | `true`  |
| `strictFunctionTypes`                | `true`  |
| `strictBindCallApply`                | `true`  |
| `strictPropertyInitialization`       | `true`  |
| `noImplicitThis`                     | `true`  |
| `alwaysStrict`                       | `true`  |
| `noUnusedLocals`                     | `true`  |
| `noUnusedParameters`                 | `true`  |
| `noImplicitReturns`                  | `true`  |
| `noFallthroughCasesInSwitch`         | `true`  |
| `noUncheckedIndexedAccess`           | `true`  |
| `noImplicitOverride`                 | `true`  |
| `noPropertyAccessFromIndexSignature` | `true`  |
| `allowUnusedLabels`                  | `false` |
| `allowUnreachableCode`               | `false` |

Types from `@cloudflare/workers-types` — **NOT** `@types/node`.

### ESLint Configuration (Zero Warnings, All Errors)

**Core TypeScript Rules:**

- `explicit-function-return-type`: error — all functions must declare return types
- `no-explicit-any`: error — no `any` types anywhere
- `strict-boolean-expressions`: error — no truthy/falsy on strings, numbers, nullable objects (`allowString: false`, `allowNumber: false`, `allowNullableObject: false`)
- `consistent-type-imports`: error — `type` keyword required for type-only imports
- `consistent-type-exports`: error
- `no-floating-promises`: error — all promises must be awaited or handled
- `no-misused-promises`: error
- `await-thenable`: error
- `prefer-nullish-coalescing`: error
- `prefer-optional-chain`: error
- `switch-exhaustiveness-check`: error
- `no-unnecessary-type-assertion`: error
- `no-unused-vars`: error (with `^_` ignore patterns)

**Naming Conventions:**

- Interfaces: PascalCase (no `I` prefix)
- Type aliases: PascalCase
- Enums: PascalCase, members: UPPER_CASE

**JSDoc Required (error level):**

- All public functions, methods, classes, interfaces, type aliases, and enums
- Requires description, param descriptions, return descriptions
- Enforces tag names, types, alignment, indentation, asterisk prefix, hyphen-before-param

**Import Ordering:**

- builtin → external → internal → parent → sibling → index
- Alphabetized within groups, newlines between groups

**Architecture Boundary Enforcement (eslint-plugin-boundaries):**

| Layer          | May Import From                             |
| -------------- | ------------------------------------------- |
| shared         | shared                                      |
| domain         | domain, shared                              |
| application    | domain, application, shared                 |
| infrastructure | domain, application, shared, infrastructure |
| presentation   | domain, application, shared, presentation   |
| di             | all                                         |
| entry          | all                                         |

**Layer-Specific Constraints:**

- **Domain:** No `throw` statements (use `Result.failure()`), no console, entities/VOs must be synchronous
- **Application:** No `throw`, no HTTP types (`Response`, `Request`, `Headers`)
- **Presentation:** No direct D1 `.prepare()`, no direct Durable Object access

**Node.js API Bans (Cloudflare Workers runtime):**

- Banned imports: `fs`, `path`, `os`, `crypto`, `child_process`, `http`, `https`, `net`, `dns`, `stream`, `buffer`, `util`, `events`, `process`
- Banned globals: `process`, `__dirname`, `__filename`, `Buffer`, `require`

**Library Containment:**

- `better-auth`: infrastructure only
- `drizzle-orm`: infrastructure only
- `hono`: presentation + worker.ts only
- `@cloudflare/workers-types`: shared/env.ts + infrastructure only

### Testing Configuration (Three Vitest Projects)

| Project      | Include Pattern                                                           | Runtime            | Coverage                |
| ------------ | ------------------------------------------------------------------------- | ------------------ | ----------------------- |
| `workers`    | `src/**/*.spec.ts`                                                        | cloudflare:workers | N/A (no node:inspector) |
| `node`       | `acceptance/**/*.spec.ts`, `scripts/**/*.spec.ts`, `.claude/**/*.spec.ts` | node               | **100% threshold**      |
| `acceptance` | `generated-acceptance-tests/**/*.spec.ts`                                 | cloudflare:workers | N/A                     |

**Coverage Thresholds (v8 provider, enforced on `node` project):**

| Metric     | Threshold |
| ---------- | --------- |
| Branches   | 100%      |
| Functions  | 100%      |
| Lines      | 100%      |
| Statements | 100%      |

Coverage applies to `acceptance/**/*.ts` (Node.js pipeline code). Workers tests run
in the Cloudflare runtime which lacks `node:inspector` — tested but not reportable.

### Acceptance Test Pipeline (ATDD)

```
specs/acceptance-specs/*.txt  (GWT format, 9 specs)
        |  parser.ts
        v
    IR (intermediate representation)
        |  generator.ts
        v
generated-acceptance-tests/*.spec.ts
```

### Pre-commit Gate (Husky + lint-staged)

Runs on every commit, in order:

1. **lint-staged** per file type:
   - `*.ts` → prettier --write, eslint --fix --max-warnings 0, tsc --noEmit, vitest related
   - `hugo/**/*.html` → hugo test suite
   - `.github/workflows/*.{yml,yaml}` → prettier + actionlint
   - `*.{json,md,yml,yaml}` → prettier
2. **knip** — unused export detection
3. **prettier** — format check (all TS files)
4. **vitest coverage** — 100% threshold on node project
5. **tsc build** — full compilation check
6. **commitlint** — conventional commit format (types: feat, fix, docs, style, refactor, perf, test, chore, revert; subject max 100 chars, lowercase, no period)

### Mutation Testing

Stryker Mutator configured with TypeScript checker and Vitest runner (`@stryker-mutator/core` 9.6.0).

### Unused Export Detection

Knip (`knip` 5.88.1) runs as part of the pre-commit gate to detect dead exports.

---

## Task Management (Beads)

SQLite-backed task tracker in `.beads/`. Integrates with the spec-kit workflow.

**Hierarchy:** Epic > Phase tasks > User story tasks > Sub-tasks
**Commands:** `br create`, `br list`, `br ready`, `br close`, `br dep add/tree`, `br stats`
**Branch convention:** Feature branches encode epic ID for CI context.

---

## Automation

### Spec-Kit Workflow (10 phases)

| Phase   | Skill               | Purpose                                   |
| ------- | ------------------- | ----------------------------------------- |
| `sp:00` | constitution        | Project principles                        |
| `sp:01` | specify             | Feature spec (creates epic + phase tasks) |
| `sp:02` | clarify             | Interactive interview                     |
| `sp:03` | plan                | Implementation plan + design artifacts    |
| `sp:04` | red-team            | Adversarial review of spec/plan           |
| `sp:05` | tasks               | Generate beads tasks from plan            |
| `sp:06` | analyze             | Cross-artifact consistency check          |
| `sp:07` | implement           | Execute tasks from beads                  |
| `sp:08` | security-review     | Security audit of changes                 |
| `sp:09` | architecture-review | Architecture compliance check             |
| `sp:10` | code-quality-review | General code quality review               |

`/sp:next` queries `br ready` and dispatches to the correct phase skill.

### Ralph (ralph.sh)

Automated loop: queries `br ready`, invokes `/sp:next`, polls for completion, repeats.
Handles phases 03-09 (01-02 are manual). Up to 50 iterations, 30-min Claude timeout,
10 retries with exponential backoff. Lock file prevents concurrent runs.

### CI/CD (GitHub Actions)

| Workflow                 | Trigger            | Purpose                                             |
| ------------------------ | ------------------ | --------------------------------------------------- |
| `ci.yml`                 | push/PR to master  | Quality gate + deploy (production or preview)       |
| `claude-code-review.yml` | PR open/sync       | 3 parallel reviews: security, architecture, quality |
| `claude.yml`             | `@claude` mentions | Claude responds to issues/PRs                       |
| `dependabot.yml`         | schedule           | Automated dependency updates                        |

---

## Skills Inventory (82)

Organized by domain:

- **Design (20, new):** design-interview, design-frontend, design-onboard, design-adapt, design-animate, design-arrange, design-audit, design-bolder, design-clarify, design-colorize, design-critique, design-delight, design-distill, design-harden, design-normalize, design-overdrive, design-polish, design-quieter, design-typeset, design-language-to-daisyui
- **Spec workflow (5):** latent-features, prefactoring, error-handling-patterns, portable-datetime, glossary
- **Architecture (4):** clean-architecture-validator, ddd-domain-modeling, static-first-routing, hypermedia-pattern-advisor
- **Workers (8):** hono-cloudflare-workers, worker-request-handler, cloudflare-project-scaffolding, cloudflare-use-case-creator, cloudflare-migrations, d1-repository-implementation, kv-session-management, cloudflare-observability
- **Testing (7):** typescript-unit-testing, vitest-cloudflare-config, vitest-integration-testing, acceptance-tests, atdd, spec-check, testing-observability
- **Hugo/Frontend (8):** hugo-project-setup, hugo-templates, hugo-copywriting, hugo-search-indexing, htmx-pattern-library, htmx-alpine-templates, typescript-html-templates, tailwind-daisyui-design, daisyui-design-system-generator
- **Review/Quality (6):** security-review, quality-review, code-review, refactoring, compound, simplify
- **Logging/Observability (4):** structured-logging, log-categorization, pii-redaction, sentry-integration
- **Automation (7):** ralph, process-pr-reviews, beads-tasks, deploy-your-app, skill-creator, skill-improver, commit
- **Content (3):** ui-design-language, blog-article-structures, hugo-copywriting
- **Content/Media (2):** web-scraper, generate-image
- **Infrastructure (4):** sprite, wrangler-sprite-auth, web-browser, summary
- **Guides (5):** start, non-technical-communication, deepen-plan, loop, claude-api

---

## Reference Documentation (19 guides)

Covering: deployment, DDD, security, auth, observability, Hugo integration,
logging, multi-tenancy, cron jobs, Slack, Stripe, TypeScript best practices,
landing pages, code review automation, library containment, and Cloudflare metrics.

---

## Affordances for Future Work

### Ready to Use (infrastructure in place)

1. **Add new routes** — Hono router in `worker.ts`, add handlers in `presentation/handlers/`
2. **Add new use cases** — Create in `application/use-cases/`, wire in `di/serviceFactory.ts`
3. **Add D1 repositories** — Implement port interfaces, use `env.DB` binding, add migrations
4. **Add new static pages** — Hugo content in `hugo/content/`, layouts in `hugo/layouts/`
5. **HTMX partials** — `/app/_/*` route reserved; add handlers behind auth middleware
6. **OAuth providers** — better-auth supports Google, GitHub, etc.; add provider config
7. **Email verification** — `verification` table exists; configure email provider
8. **Acceptance specs** — Write GWT in `specs/acceptance-specs/`, run `just acceptance`
9. **Automated feature loop** — `./ralph.sh` for hands-off phase 03-09 execution
10. **Design workflow** — `/design-interview` orchestrates full design pass with 20 specialized skills

### Available but Not Yet Bound

11. **R2 object storage** — Add binding in `wrangler.toml`, implement adapter
12. **Cron triggers** — Add `[triggers]` in `wrangler.toml`, implement scheduled handler
13. **Queue consumers** — Cloudflare Queues for async work
14. **Search** — Hugo search indexing skill + D1 FTS5
15. **Multi-tenancy** — Skills and guides ready; needs schema + middleware
16. **Observability** — SLO/SLI metrics, structured logging skills ready
17. **Sentry integration** — Skill available, needs configuration
18. **Stripe payments** — Guide and skill available
19. **Slack bot** — Guide and skill available

### Requires Configuration Only

20. **Production URL** — Set `baseURL` in `hugo/hugo.yaml` (marked STARTUPFIXME)
21. **Site identity** — params.yaml: title, description, logo, author, social links
22. **Google Analytics** — Set `googleAnalytics` in params.yaml
23. **Cloudflare secrets** — `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` via `wrangler secret put`
24. **GitHub secrets** — `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` for CI deploy

---

## File Counts

| Category            | Count | Change from Mar 19 |
| ------------------- | ----- | ------------------ |
| Source files (src)  | 44    | -4                 |
| Test files (src)    | 41    | -1                 |
| Skills              | 82    | +16                |
| Acceptance specs    | 9     | —                  |
| Reference docs      | 19    | +1                 |
| Hugo layouts        | 16    | —                  |
| Migrations          | 1     | —                  |
| Source lines        | 5,088 |                    |
| Test lines          | 8,286 |                    |
| Acceptance pipeline | 1,320 |                    |

---

## Changes Since Last Snapshot (2026-03-19)

- **013-import-design-skills (PR #59, merged):** Imported 20 design skills from
  pbakaus/impeccable, adapted for TailwindCSS 4 + DaisyUI 5 + Hugo stack.
  Added `design-interview` orchestrator, `design-frontend` reference hub with
  7 reference docs, NOTICE file for Apache 2.0 compliance. Updated CLAUDE.md
  with design skills section. Fixed `br` CLI compatibility in ralph.sh and
  sp commands. Skills count grew from 66 to 82.

---

## Key Commands Quick Reference

```bash
# Development
just dev-worker          # Local Worker dev server
just hugo-dev            # Hugo dev server (localhost:1313)
npx vitest               # TDD watch mode

# Quality
just check               # type-check + lint + test
just ci                  # Full CI pipeline locally
npm run fix              # Auto-fix formatting + linting

# Testing
just test                # Unit tests
just test-all            # Unit + acceptance
just acceptance          # Full ATDD pipeline

# Deploy
just deploy              # Build Hugo + deploy Worker

# Automation
./ralph.sh               # Automated spec-to-deploy loop
```
