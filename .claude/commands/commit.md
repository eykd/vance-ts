---
description: Commit the current changeset to git.
---

Commit all current changes in the git repository. Be sure that the commit message covers all staged changes, not just the most recent work.

Use conventional commit style in the commit message.
