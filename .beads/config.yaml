# Beads Configuration File (beads_rust / br)
# This file configures default behavior for all br commands in this repository
# Settings can also be set via environment variables or overridden with CLI flags

# Issue prefix for this repository (used by br init)
# If not set, br init will auto-detect from directory name
# issue-prefix: ""

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by --actor flag)
# actor: ""
