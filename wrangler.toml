name = "vance-ts"
compatibility_date = "2024-01-01"
pages_build_output_dir = "hugo/public"

# Note: For Cloudflare Pages projects, D1, KV, and R2 bindings
# are configured in the Cloudflare dashboard under:
# Workers & Pages > vance-ts > Settings > Functions > Bindings
